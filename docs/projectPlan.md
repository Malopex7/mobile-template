# Project Plan

When using this template for a new project:
1. Search and replace `expo-express-monorepo` with the new project name.
2. Configure your MongoDB Atlas connection string.
3. Update `apps/mobile/app.json` with new bundle identifiers and icons.
4. Establish Supabase Storage credentials if using the asset upload flow.
